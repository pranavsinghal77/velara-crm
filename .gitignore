# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Credential notes kept locally. This project has already leaked a database
# password through a committed file once (see commit decb964, server/test_db.js),
# so anything holding a credential is ignored by name rather than by memory.
CREDENTIALS.txt
credentials.txt
*.credentials
*.secrets.txt
